- command: update
  path: jobs.build.steps
  value:
  - name: build env.CONTAINER @ env.TAG
  - uses: actions/checkout@master
    with:
      repository: arthur-stace/env.CONTAINER
    env:
      PROJECT: env.PROJECT
    run: docker build env.CONTAINER --tag env.CONTAINER:env.TAG
