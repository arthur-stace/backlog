- command: update
  path: jobs.build.steps
  value:
  - name: build env.DOMAIN @ env.TAG
  - uses: actions/checkout@master
    with: arthur-stace/env.DOMAIN
    runs-on: ubuntu-latest
    env:
      PROJECT: env.PROJECT
    run: docker build env.DOMAIN --tag env.DOMAIN:env.TAG
