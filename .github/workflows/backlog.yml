name: data
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build:
    steps:
    - name: build yaaghs @ 1593557475
    - uses: actions/checkout@master
      with: arthur-stace/yaaghs
      runs-on: ubuntu-latest
      env:
        PROJECT: arthur-stace/rails
      run: docker build yaaghs --tag yaaghs:1593557475
