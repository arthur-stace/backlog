name: backlog
on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
jobs:
  build:
    steps:
    - name: build yaaghs @ 1593566360
    - uses: actions/checkout@v2
      run: docker build yaaghs --tag yaaghs:1593566360
